"""FairEntry — a transparent, data-first stock-decision platform.

Pull data once into a canonical store; every screener reads from the store;
a config-driven, fully transparent scoring model produces Buy / Watch / Avoid.
"""
__version__ = "0.1.0"
