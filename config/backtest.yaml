version: 1
name: fairentry_evidence_v1
universe:
  mode: seeded
  screened_only: true
execution:
  entry: snapshot_close
  target_hit: close
  slippage_bps: 10
  transaction_cost_bps: 5
benchmark:
  mode: cohort_mean
horizons_days: [30, 60, 90, 180, 365]
target:
  models: [practical, fundamental, technical, blended, fcf, book, peer_pe, peer_ps, analyst, lynch]
  primary: practical
  expiry_days: 365
  minimum_upside_pct: 30
  maximum_upside_pct: 100
data_quality:
  # The current seeded artifact contains many current-proxy fundamentals.
  # Choose experimental explicitly; mostly/strict now enforce source exclusion.
  mode: experimental
  strict_excluded_sources: [seed_const]
tuning:
  promotion: manual
  challenger_holds_days: [20, 30, 60, 90, 180]
